% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Rfunc.R
\name{debias_gic_select}
\alias{debias_gic_select}
\title{GIC select with debias term}
\usage{
debias_gic_select(
  parameters,
  n,
  an,
  u,
  covx,
  p,
  start,
  ori,
  weight,
  s,
  L = 1.2,
  alpha = 0.11,
  iter = 200,
  tol = 1e-06
)
}
\arguments{
\item{parameters}{a matrix where each row contains a combination of gamma, lambda1, and lambda2 to evaluate with at most 3 gammas.}

\item{n}{the number of samples}

\item{an}{the penalty adjustment function used in GIC}

\item{u}{the U-statistics vector}

\item{covx}{the covariance matrix of the predictors}

\item{p}{the number of predictors}

\item{start}{the starting point for the optimization}

\item{ori}{the original data or parameter used in the optimization process}

\item{weight}{the weights applied in the lasso optimization}

\item{s}{scaling factor for the solution}

\item{L}{Lipschitz constant used in the optimization algorithm}

\item{alpha}{the learning rate for the optimization}

\item{iter}{the maximum number of iterations for the optimization}

\item{tol}{the tolerance level for convergence}
}
\value{
the debiased coefficients selected by the best combination of gamma, lambda1, and lambda2
}
\description{
Use GIC (Generalized Information Criterion) to select the best parameters with a debias term
}
\examples{
\dontrun{
parameters <- matrix(c(0.5, 0.1, 0.01, 1, 0.2, 0.02), ncol = 3, byrow = TRUE)
debias_gic_select(parameters, n = 100, an = log,
                  u = c(1, 2), covx = matrix(c(1, 0.5, 0.5, 1), 2, 2),
                  p = 2, start = c(1, 1), ori = c(1, 1),
                  weight = c(1, 1), s = 1)
}
}
